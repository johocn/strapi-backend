# 后台管理系统使用手册

> 版本：2.0.0 | 基于 UniApp + Strapi | 面向管理员用户 | 详细操作指南

---

## 目录

- [1. 系统概述](#1-系统概述)
- [2. 登录与权限](#2-登录与权限)
- [3. 仪表盘](#3-仪表盘)
- [4. 多租户管理](#4-多租户管理)
- [5. 渠道管理](#5-渠道管理)
- [6. 课程管理](#6-课程管理)
- [7. 网站内容管理](#7-网站内容管理)
- [8. 题库管理](#8-题库管理)
- [9. 积分管理](#9-积分管理)
- [10. 系统管理](#10-系统管理)
- [11. 媒体管理](#11-媒体管理)
- [12. 第三方配置](#12-第三方配置)
- [13. 常见问题](#13-常见问题)
- [14. 官网中心学习路径](#14-官网中心学习路径)

---

## 1. 系统概述

### 1.1 系统架构

本系统是基于 Strapi + UniApp 构建的多租户管理后台，主要功能模块包括：

```
后台管理系统
├── 多租户管理 — 创建/编辑/删除租户，配置站点信息、三方登录、模板样式
├── 渠道管理 — 渠道树结构，成员管理，邀请码，渠道网络查看
├── 课程管理 — 课程/课时/分类/标签/知识点管理，渠道权限控制
├── 题库管理 — 题目管理，答题记录，考试管理，批量导入
├── 积分管理 — 积分类型/规则/记录/兑换管理，统计报表
├── 系统管理 — 用户/角色/权限/日志管理
├── 媒体管理 — 文件上传，图片库管理
├── 第三方配置 — 微信登录/分享配置，支付宝/抖音登录
└── 仪表盘 — 数据统计概览
```

### 1.2 浏览器要求

| 浏览器 | 最低版本 | 说明 |
|--------|----------|------|
| Chrome | 90+ | 推荐 |
| Firefox | 88+ | |
| Edge | 90+ | |

### 1.3 访问地址

- **开发环境**：http://localhost:1337/admin
- **生产环境**：http://你的域名/admin

**域名规划**：

| 域名 | 角色 | 说明 |
|------|------|------|
| h.joho.cn | 后台管理 + SSO 中转 | Strapi Admin 面板、SSO 微信网页授权回调、JSSDK 签名 |
| v.joho.cn | C 端课程内容 | uni-app H5 课程站点，承载 invite_code 参数 |

**SSO 相关入口**：

- SSO 插件管理：`http://h.joho.cn/admin/plugins/zhao-sso`
- SSO 登录页：`http://h.joho.cn/#/pages/sso/login`
- SSO 回调页：`http://h.joho.cn/#/pages/sso/login-callback`
- 微信授权跳转：`http://h.joho.cn/api/zhao-sso/v1/auth/wechat`
- JSSDK 签名接口：`POST http://h.joho.cn/api/zhao-sso/v1/auth/jssdk-signature`

### 1.4 角色权限说明

| 角色 | 权限范围 | 适用场景 |
|------|----------|----------|
| super-admin | 全部权限 | 系统管理员 |
| admin | 除系统设置外的全部权限 | 租户管理员 |
| channel-admin | 渠道管理权限 | 渠道管理员 |
| editor | 内容编辑权限 | 内容编辑 |
| viewer | 只读权限 | 查看者 |

---

## 2. 登录与权限

### 2.1 登录流程

1. 打开浏览器，访问后台地址
2. 在登录页面输入用户名和密码
3. 点击「登录」按钮

**默认账号**：
- 用户名：admin
- 密码：首次启动时设置

### 2.2 权限说明

- 系统会根据用户角色自动过滤可访问的菜单和操作
- 无权限的菜单会自动隐藏
- 无权限的操作会返回 403 错误
- 管理员用户拥有全部渠道权限

---

## 3. 仪表盘

### 3.1 功能说明

仪表盘展示系统核心数据统计：

| 统计项 | 说明 |
|--------|------|
| 用户总数 | 系统注册用户总数 |
| 课程总数 | 已发布课程数量 |
| 渠道总数 | 渠道数量 |
| 今日活跃 | 今日活跃用户数 |
| 学习进度 | 课程学习进度统计 |
| 积分排行 | 积分排行榜 |

### 3.2 操作指南

1. 登录后自动进入仪表盘页面
2. 点击统计卡片可查看详细数据
3. 使用时间筛选器查看不同时间段的数据

---

## 4. 多租户管理

### 4.1 功能概述

多租户管理用于管理系统中的多个租户（站点），每个租户拥有独立的渠道、课程和配置。支持：
- 创建/编辑/删除租户
- 配置站点基本信息（名称、域名、Logo等）
- 设置SEO信息
- 配置分享参数
- 开启/关闭功能开关
- 设置认证模式（本地登录/三方登录/SSO）
- 关联渠道
- 配置三方登录（微信/支付宝/抖音）
- 设置模板样式和主题色

### 4.2 创建租户

**操作步骤**：

1. 点击左侧菜单「多租户管理」→「租户列表」
2. 点击「新建租户」按钮
3. 填写租户基本信息：

**基本信息字段说明：**

| 字段名称 | 必填 | 说明 | 示例 |
|----------|------|------|------|
| 租户名称 | * | 站点名称，显示在页面标题和分享中 | 我的教育平台 |
| 域名 | * | 站点访问域名，用于区分不同租户 | example.com |
| 租户描述 | | 站点描述信息 | 专注于职业技能培训的在线教育平台 |
| ICP备案号 | | 网站备案号，用于合规展示 | 京ICP备12345678号 |
| 客服链接 | | 客服入口链接，如企业微信 | https://work.weixin.qq.com/... |

4. 设置站点标识：

**站点标识字段说明：**

| 字段名称 | 必填 | 说明 | 建议尺寸 |
|----------|------|------|----------|
| 站点 Logo | | 站点Logo图片，显示在页面顶部 | 200x200px |
| Favicon | | 浏览器标签图标 | 32x32px |

5. 配置SEO设置：

**SEO设置字段说明：**

| 字段名称 | 必填 | 说明 | 示例 |
|----------|------|------|------|
| SEO关键词 | | 搜索引擎关键词，多个用逗号分隔 | 在线教育,职业培训,技能学习 |
| SEO描述 | | 搜索引擎展示的描述文字 | 专业的在线职业技能培训平台 |
| 腾讯地图Key | | 腾讯位置服务API Key | XXXXXXXX |

6. 配置分享设置：

**分享设置字段说明：**

| 字段名称 | 必填 | 说明 | 示例 |
|----------|------|------|------|
| 分享标题 | | 转发朋友圈时的标题 | 快来学习这门课程！ |
| 分享描述 | | 转发朋友圈时的描述 | 专业讲师授课，助你快速成长 |
| 分享封面图 | | 分享时显示的封面图片 | 推荐尺寸 500x400px |
| 分享路径 | | 分享链接指向的页面路径 | /pages/index/index |

7. 设置功能开关：

**功能开关说明：**

| 开关名称 | 默认值 | 说明 |
|----------|--------|------|
| SSO登录 | 关闭 | 启用单点登录功能 |
| 积分系统 | 开启 | 启用积分奖励系统 |
| 题库管理 | 开启 | 启用题库和考试功能 |
| 课程管理 | 开启 | 启用课程管理功能 |
| 渠道管理 | 开启 | 启用渠道管理功能 |
| 三方登录 | 开启 | 启用微信/支付宝/抖音登录 |
| OSS存储 | 关闭 | 启用阿里云OSS存储 |

8. 配置认证模式：

**认证模式说明：**

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| 本地登录 | 使用账号密码或手机验证码登录 | 独立站点，无需第三方登录 |
| 三方登录 | 使用微信/支付宝/抖音登录 | 需要社交账号登录 |
| SSO单点登录 | 使用企业SSO系统登录 | 企业内部集成 |

**三方登录平台选择（认证模式为"三方登录"时显示）：**

| 平台 | 说明 |
|------|------|
| 微信公众号 | 微信网页授权登录 |
| 微信小程序 | 微信小程序登录 |
| 微信开放平台 | 微信开放平台登录 |
| 支付宝 | 支付宝登录 |
| 抖音 | 抖音登录 |

**SSO配置（认证模式为"SSO"时显示）：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| SSO登录地址 | * | SSO登录页面URL |

**注册设置：**

| 字段名称 | 默认值 | 说明 |
|----------|--------|------|
| 允许注册 | 关闭 | 是否允许用户自主注册 |
| 必填邀请码 | 关闭 | 注册时是否必须填写邀请码 |

9. 配置渠道（**必选**）：

**渠道配置说明：**

| 设置项 | 说明 |
|--------|------|
| 是否允许跨渠道 | 开启后用户可见跨渠道课程/分类，关闭后仅展示站点渠道数据 |
| 关联渠道 | 选择租户关联的渠道，只能选择你有权限的渠道 |

**操作步骤：**
- 点击「+ 添加渠道」按钮
- 在弹出的渠道选择器中选择要关联的渠道（可多选）
- 点击渠道名称左侧的复选框进行选择/取消选择
- 点击「确定」按钮确认选择

10. 点击「保存并继续」按钮保存租户信息

**注意事项：**
- 租户名称和域名为必填项，必须填写
- 渠道为必选项，必须至少选择一个渠道
- 保存后会自动生成租户的documentId，用于后续配置
- 保存后页面会刷新，进入编辑模式

11. 配置三方登录（保存租户后）：

**操作步骤：**
- 在「三方登录配置」区域点击「+ 添加配置」
- 填写配置信息：

**三方登录配置字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 配置名称 | * | 配置的显示名称 |
| 平台 | * | 选择平台：微信、支付宝、抖音 |
| 应用类型 | * | 选择应用类型：公众号、小程序、开放平台等 |
| App ID/AppID/Client Key | * | 对应平台的应用ID |
| App Secret/应用私钥/Client Secret | * | 对应平台的应用密钥 |
| 商户号 | | 支付宝平台需要填写 |
| Token | | 微信平台需要填写 |
| 启用状态 | | 是否启用该配置 |

- 点击「保存」按钮保存配置
- 配置会显示在三方登录配置列表中，可进行编辑和删除

12. 配置模板样式：

**模板样式字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 预设模板 | | 选择预设的站点模板 |
| 主题色 | | 站点主色调，影响按钮、导航栏等 |
| 辅助色 | | 辅助颜色，用于强调和次要元素 |
| 导航样式 | | 默认、渐变、自定义 |
| tabBar颜色 | | 底部导航栏背景色 |
| tabBar激活色 | | 底部导航栏激活状态颜色 |

### 4.3 编辑租户

**操作步骤**：

1. 在租户列表中点击要编辑的租户名称
2. 修改租户信息（参考创建租户的字段说明）
3. 如需修改三方登录配置：
   - 点击配置项右侧的「编辑」按钮
   - 修改配置信息
   - 点击「保存」按钮
4. 如需删除三方登录配置：
   - 点击配置项右侧的「删除」按钮
   - 在确认对话框中点击「确定」
5. 点击「保存」按钮保存修改，或点击「保存并返回」按钮保存并返回列表

### 4.4 删除租户

**操作步骤**：

1. 在租户列表中找到要删除的租户
2. 点击「删除」按钮
3. 在确认对话框中点击「确定」

> **注意**：删除租户会同时删除关联的站点配置和三方配置，此操作不可恢复，请谨慎操作。

### 4.5 常见问题与错误提示

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 提示"请填写租户名称和域名" | 租户名称或域名未填写 | 填写租户名称和域名 |
| 提示"请选择至少一个渠道" | 未选择关联渠道 | 点击「+ 添加渠道」选择渠道 |
| 提示"请先保存租户信息" | 在新建租户时未保存就尝试添加三方配置 | 先点击「保存并继续」保存租户 |
| 提示"请填写SSO登录地址" | 认证模式为SSO但未填写SSO地址 | 填写SSO登录地址 |
| 三方配置列表不显示 | 新建租户后未刷新页面 | 保存租户后等待页面自动刷新 |

---

## 5. 渠道管理

### 5.1 功能概述

渠道管理用于管理系统的渠道层级结构，支持多级渠道、成员管理和邀请码机制。

### 5.2 渠道层级

```
平台根渠道
├── 省级渠道
│   ├── 市级渠道
│   │   └── 个人渠道
│   └── 市级渠道
└── 省级渠道
    └── ...
```

**渠道层级说明：**

| 层级代码 | 显示名称 | 说明 |
|----------|----------|------|
| root | 根渠道 | 平台级渠道 |
| national | 全国 | 国家级渠道 |
| provincial | 省级 | 省级渠道 |
| city | 城市 | 市级渠道 |
| county | 区县 | 区县级渠道 |
| local | 本地 | 本地渠道 |
| store | 门店 | 门店级渠道 |
| core | 核心渠道 | 核心合作渠道 |
| senior | 高级渠道 | 高级渠道 |
| global | 全球渠道 | 全球渠道 |
| authorized | 授权渠道 | 授权渠道 |
| official | 官方渠道 | 官方渠道 |
| partner | 合作伙伴 | 合作伙伴渠道 |
| agent | 代理渠道 | 代理商渠道 |

### 5.3 创建渠道

**操作步骤**：

1. 点击左侧菜单「渠道管理」→「渠道列表」
2. 点击「+ 新增」按钮或右下角的「+」悬浮按钮
3. 填写渠道信息：

**渠道信息字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 渠道名称 | * | 渠道的显示名称 |
| 代码 | | 渠道的唯一代码（系统自动生成） |
| 描述 | | 渠道的描述信息 |
| 层级 | * | 选择渠道层级（root/national/provincial/city等） |
| 父渠道 | | 选择上级渠道（可选，创建子渠道时必填） |

4. 点击「保存」按钮

### 5.4 查看渠道列表

**操作步骤**：

1. 点击左侧菜单「渠道管理」→「渠道列表」
2. 在列表中查看所有渠道信息

**列表字段说明：**

| 字段名称 | 说明 |
|----------|------|
| 渠道名称 | 渠道的显示名称 |
| 代码 | 渠道的唯一代码 |
| 描述 | 渠道的描述信息 |
| 状态 | 启用/禁用 |
| 层级 | 渠道层级 |
| 上级 | 上级渠道名称 |

**筛选功能：**

| 筛选条件 | 说明 |
|----------|------|
| 搜索框 | 按渠道名称搜索 |
| 层级筛选 | 按渠道层级筛选 |
| 状态筛选 | 按启用/禁用状态筛选 |

### 5.5 查看渠道网络

**操作步骤**：

1. 在渠道列表页面点击顶部的「网络」按钮
2. 在弹出的窗口中查看渠道树结构
3. 可以展开/折叠各级渠道查看完整层级

### 5.6 管理渠道成员

**操作步骤**：

1. 在渠道列表中找到要管理的渠道
2. 点击「成员」按钮
3. 查看成员列表
4. 点击「邀请成员」添加新成员
5. 点击「移除成员」删除成员

### 5.7 编辑渠道

**操作步骤**：

1. 在渠道列表中找到要编辑的渠道
2. 点击「编辑」按钮
3. 修改渠道信息
4. 点击「保存」按钮

### 5.8 删除渠道

**操作步骤**：

1. 在渠道列表中找到要删除的渠道
2. 点击「删除」按钮
3. 在确认对话框中点击「确定」

> **注意**：删除渠道会级联删除所有子渠道和成员，此操作不可恢复，请谨慎操作。

### 5.9 生成邀请码

**操作步骤**：

1. 进入渠道详情页
2. 在「邀请码」区域点击「生成邀请码」
3. 复制邀请码分享给他人

### 5.10 常见问题与错误提示

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 无法创建子渠道 | 未选择父渠道 | 在创建渠道时选择父渠道 |
| 删除渠道失败 | 渠道下有子渠道或成员 | 先删除子渠道和成员，再删除当前渠道 |
| 搜索不到渠道 | 关键词输入错误 | 检查搜索关键词是否正确 |

---

## 6. 课程管理

### 6.1 功能概述

课程管理用于管理系统中的课程内容，包括课程、课时、分类、标签和知识点。支持：
- 创建/编辑/删除课程
- 配置课程基本信息
- 设置课程价格（免费/付费/折扣）
- 关联课程分类和标签
- 关联知识点
- 设置渠道权限范围
- 配置积分奖励
- 设置课程时间和状态

### 6.2 创建课程

**操作步骤**：

1. 点击左侧菜单「课程管理」→「课程列表」
2. 点击「+ 新增课程」按钮
3. 填写课程基本信息：

**基本信息字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 课程名称 | * | 课程的显示名称 |
| 课程描述 | | 课程的详细描述 |
| 作者 | | 课程作者名称 |

4. 上传媒体资源：

**媒体资源字段说明：**

| 字段名称 | 必填 | 说明 | 建议尺寸 |
|----------|------|------|----------|
| 封面图 | | 课程封面图片 | 800x600px |
| 缩略图 | | 课程缩略图 | 200x200px |

**上传操作步骤：**
- 点击上传区域
- 在弹出的媒体选择器中选择图片
- 图片会自动上传并显示预览

5. 设置课程价格：

**价格设置字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 免费课程 | | 开启后课程免费 |
| 付费课程 | | 开启后课程收费 |
| 现价（分） | | 当前售价，单位为分 |
| 原价（分） | | 原价，单位为分 |
| 折扣价（分） | | 折扣价，单位为分 |

> **注意**：价格单位为分，如100分 = 1元

6. 设置分类和标签：

**分类标签字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 课程分类 | | 选择课程所属分类 |
| 课程标签 | | 添加课程标签（可多选） |

**操作步骤：**
- 点击「课程分类」选择器，选择分类
- 点击「+ 添加标签」，在弹出的标签选择器中选择标签
- 点击标签右侧的「×」按钮移除标签

7. 关联知识点：

**操作步骤：**
- 点击「+ 添加知识点」
- 在弹出的知识点选择器中选择知识点（可多选）
- 点击知识点右侧的「×」按钮移除知识点

> **说明**：知识点是系统中预定义的知识体系节点，用于课程内容的结构化管理

8. 设置学习参数：

**学习设置字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 难度级别 | | beginner（初级）/ intermediate（中级）/ advanced（高级）/ expert（专家） |
| 课程等级 | | introductory（入门）/ foundation（基础）/ advanced（进阶）/ professional（专业） |
| 课程时长（分钟） | | 课程总时长 |
| 排序 | | 课程在列表中的排序优先级，数值越小越靠前 |

9. 设置渠道范围：

**渠道设置字段说明：**

| 设置项 | 说明 |
|--------|------|
| 全部渠道 | 课程对所有渠道公开，跨渠道可见 |
| 指定渠道 | 课程仅对指定渠道的成员可见，渠道专属课程 |
| 积分归属渠道 | 指定渠道模式下，学习本课程获得的积分归属此渠道 |
| 允许跨渠道访问 | 关闭后，游客无法浏览此课程 |

**操作步骤（指定渠道模式）：**
- 选择「指定渠道」选项
- 点击「点击选择渠道」，在弹出的渠道选择器中选择渠道（可多选）
- 点击「点击选择积分归属渠道」，在已选渠道中选择积分归属渠道（单选）

> **注意**：指定渠道模式下，积分归属渠道必须在所属渠道中选择

10. 设置积分奖励（如果积分系统已启用）：

**积分设置字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 启用积分 | | 是否启用课程积分奖励 |
| 积分数量 | | 完成课程可获得的积分数量 |
| 积分类型 | | course_points（课程积分）/ lesson_points（课时积分） |

11. 设置时间信息：

**时间设置字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 报名开始日期 | | 课程报名开始时间 |
| 报名结束日期 | | 课程报名结束时间 |
| 课程开始日期 | | 课程正式开始时间 |
| 课程结束日期 | | 课程结束时间 |

12. 设置课程状态：

**状态管理字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 课程状态 | | draft（草稿）/ pending（待审核）/ published（已发布）/ archived（已归档） |
| 审核状态 | | pending（待审核）/ approved（已通过）/ rejected（已拒绝） |
| 精选课程 | | 是否设为精选课程，精选课程会在首页优先展示 |

13. 点击「保存课程」按钮

### 6.3 编辑课程

**操作步骤**：

1. 在课程列表中点击要编辑的课程名称
2. 修改课程信息（参考创建课程的字段说明）
3. 点击「保存课程」按钮

### 6.4 删除课程

**操作步骤**：

1. 在课程列表中找到要删除的课程
2. 点击「删除」按钮
3. 在确认对话框中点击「确定」

### 6.5 添加课时

**操作步骤**：

1. 进入课程详情页
2. 点击「添加课时」按钮
3. 填写课时信息：

**课时信息字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 课时标题 | * | 课时的显示名称 |
| 类型 | * | video（视频）/ audio（音频）/ article（文章）/ quiz（测验） |
| 内容链接 | | 视频或音频链接 |
| 描述 | | 课时简介 |
| 排序 | | 课时在课程中的顺序 |

4. 点击「保存」按钮

### 6.6 课程分类管理

**操作步骤**：

1. 点击左侧菜单「课程管理」→「课程分类」
2. 点击「新建分类」按钮
3. 填写分类名称和描述
4. 点击「保存」按钮

### 6.7 课程标签管理

**操作步骤**：

1. 点击左侧菜单「课程管理」→「课程标签」
2. 点击「新建标签」按钮
3. 填写标签名称
4. 点击「保存」按钮

### 6.8 知识点管理

**操作步骤**：

1. 点击左侧菜单「课程管理」→「知识点」
2. 点击「新建知识点」按钮
3. 填写知识点名称和描述
4. 选择所属学科
5. 设置难度级别
6. 点击「保存」按钮

### 6.9 常见问题与错误提示

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 提示"请输入课程名称" | 课程名称未填写 | 填写课程名称 |
| 提示"指定渠道模式下，请至少选择1个所属渠道" | 指定渠道模式下未选择渠道 | 在渠道设置中选择至少一个渠道 |
| 提示"请选择积分归属渠道" | 指定渠道模式下未选择积分归属渠道 | 选择积分归属渠道 |
| 提示"积分归属渠道必须在所属渠道中" | 积分归属渠道不在已选渠道中 | 重新选择积分归属渠道，必须从已选渠道中选择 |
| 课程发布后不可见 | 课程状态为草稿或待审核 | 将课程状态改为已发布 |

---

## 7. 网站内容管理

### 7.1 功能概述

网站内容管理用于管理面向官网公开展示的 7 类内容（Content Type，简称 CT），所有 CT 遵循统一的状态流转和操作模式。支持：

- 创建/编辑/删除内容
- 统一的状态流转：draft（草稿）→ published（已发布）→ archived（已下架）
- 列表页搜索 + 状态筛选 + 分页
- 编辑页基本信息 + 内容区 + 扩展区结构
- JSON 字段（features/specifications/scenarios/results/steps/materials）容错解析

**CT 列表：**

| CT | 名称 | 主字段 | 说明 |
|----|------|--------|------|
| article | 资讯文章 | title | 公司新闻、行业动态、活动公告 |
| product | 产品 | name | 产品介绍、特性规格、应用场景 |
| case | 落地案例 | title | 客户案例、挑战与解决方案 |
| faq | 常见问答 | question | 常见问题与解答 |
| tutorial | 教程指南 | title | 产品使用教程、操作指南 |
| compliance | 合规公示 | title | 法律政策、资质证书、协议文件 |
| download | 下载资源 | name | 白皮书、宣传册、模板等下载文件 |

### 7.2 通用操作流程

所有 CT 遵循统一的操作流程：

**列表页操作：**

| 操作 | 说明 |
|------|------|
| 搜索 | 按主字段（title/name/question）关键字搜索 |
| 状态筛选 | 按 draft/published/archived 筛选 |
| 分页 | 翻页浏览，默认每页 10 条 |
| 卡片列表 | 以卡片形式展示，包含主字段、状态、更新时间 |
| 新增 | 点击「+ 新增」按钮进入编辑页 |

**编辑页结构：**

| 区域 | 说明 |
|------|------|
| 基本信息区 | 主字段、摘要、封面图等核心信息 |
| 内容区 | 正文、JSON 字段等主要内容 |
| 扩展区 | 状态、排序、置顶、精选等扩展属性 |

**发布流程：**

1. 在编辑页填写内容
2. 点击「存草稿」保存为 draft 状态
3. 内容确认后点击「发布」切换到 published 状态
4. 内容过期或需要下线时点击「下架」切换到 archived 状态
5. 在列表页可执行编辑、发布、下架、删除操作

### 7.3 article 资讯文章

**操作步骤：**

1. 进入「网站管理 → 资讯文章」页面
2. 点击「+ 新增文章」按钮
3. 填写标题、正文、分类、标签等字段
4. 点击「存草稿」或「发布」按钮
5. 在列表页可编辑、发布、下架、删除

**article 字段说明：**

| 字段名称 | 类型 | 必填 | 说明 |
|----------|------|------|------|
| title | string | * | 标题 |
| slug | uid | * | URL 别名 |
| excerpt | text | | 摘要 |
| content | text | * | 正文 |
| coverImage | media | | 封面图 |
| category | relation | | 分类 |
| tags | relation | | 标签 |
| status | enum | * | draft/published/archived |

### 7.4 product 产品

**操作步骤：**

1. 进入「网站管理 → 产品」页面
2. 点击「+ 新增产品」按钮
3. 填写产品名称、标语、特性、规格等字段
4. 点击「存草稿」或「发布」按钮

**product 字段说明：**

| 字段名称 | 类型 | 必填 | 说明 |
|----------|------|------|------|
| name | string | * | 产品名称 |
| tagline | string | | 标语 |
| features | json | | 核心特性（JSON 数组） |
| specifications | json | | 规格参数（JSON 对象） |
| scenarios | json | | 应用场景（JSON 数组） |
| isFeatured | boolean | | 是否精选 |
| status | enum | * | draft/published/archived |

> **注意**：product 主字段为 `name`，不是 `title`，列表页搜索也按 name 进行。

### 7.5 case 落地案例

**操作步骤：**

1. 进入「网站管理 → 落地案例」页面
2. 点击「+ 新增案例」按钮
3. 填写客户名称、挑战、解决方案、成果等字段
4. 点击「存草稿」或「发布」按钮

**case 字段说明：**

| 字段名称 | 类型 | 必填 | 说明 |
|----------|------|------|------|
| title | string | * | 案例标题 |
| clientName | string | * | 客户名称 |
| clientIndustry | string | | 客户行业 |
| challenge | text | * | 挑战 |
| solution | text | * | 解决方案 |
| results | json | * | 成果（JSON 数组） |
| testimonial | text | | 客户证言 |
| status | enum | * | draft/published/archived |

### 7.6 faq 常见问答

**操作步骤：**

1. 进入「网站管理 → 常见问答」页面
2. 点击「+ 新增问答」按钮
3. 填写问题和答案
4. 点击「存草稿」或「发布」按钮

**faq 字段说明：**

| 字段名称 | 类型 | 必填 | 说明 |
|----------|------|------|------|
| question | text | * | 问题 |
| answer | text | * | 答案 |
| order | integer | | 排序 |
| isFeatured | boolean | | 是否精选 |
| status | enum | * | draft/published/archived |

> **注意**：faq 主字段为 `question`，没有 `title`、`slug`、`coverImage` 字段，列表页按 question 搜索。

### 7.7 tutorial 教程指南

**操作步骤：**

1. 进入「网站管理 → 教程指南」页面
2. 点击「+ 新增教程」按钮
3. 填写标题、操作步骤、所需材料等字段
4. 点击「存草稿」或「发布」按钮

**tutorial 字段说明：**

| 字段名称 | 类型 | 必填 | 说明 |
|----------|------|------|------|
| title | string | * | 标题 |
| steps | json | * | 操作步骤（JSON 数组） |
| materials | json | | 所需材料（JSON 数组） |
| difficulty | enum | | beginner/intermediate/advanced |
| estimatedTime | string | | 预计耗时 |
| status | enum | * | draft/published/archived |

### 7.8 compliance 合规公示

**操作步骤：**

1. 进入「网站管理 → 合规公示」页面
2. 点击「+ 新增合规」按钮
3. 填写标题、分类、正文等字段
4. 点击「存草稿」或「发布」按钮

**compliance 字段说明：**

| 字段名称 | 类型 | 必填 | 说明 |
|----------|------|------|------|
| title | string | * | 标题 |
| category | enum | * | notice/policy/report/certificate/agreement |
| content | text | * | 正文 |
| effectiveDate | date | | 生效日期 |
| expiryDate | date | | 有效期至 |
| isPinned | boolean | | 是否置顶 |
| status | enum | * | draft/published/archived |

> **注意**：compliance 的 `category` 是字段级枚举（notice/policy/report/certificate/agreement），不是 relation 关联，与 article 的 category 不同。

### 7.9 download 下载资源

**操作步骤：**

1. 进入「网站管理 → 下载资源」页面
2. 点击「+ 新增下载」按钮
3. 填写资源名称、上传文件、设置文件类型
4. 点击「存草稿」或「发布」按钮

**download 字段说明：**

| 字段名称 | 类型 | 必填 | 说明 |
|----------|------|------|------|
| name | string | * | 资源名称 |
| file | media | * | 文件 |
| fileType | enum | | whitepaper/brochure/datasheet/template/guide/certificate/other |
| requireLead | boolean | | 是否需要留资（默认 true） |
| downloadCount | biginteger | | 下载次数 |
| status | enum | * | draft/published/archived |

> **注意**：download 主字段为 `name`，没有 `slug` 字段；`requireLead` 默认为 true，即用户下载前需提交联系信息。

### 7.10 JSON 字段编辑说明

features、specifications、scenarios、results、steps、materials 等字段使用 textarea 编辑 JSON 内容。

**输入规范：**

| 规则 | 说明 |
|------|------|
| 字符串 | 必须使用双引号 `"`，不能使用单引号 |
| 数组 | 使用方括号 `[]`，元素用逗号分隔 |
| 对象 | 使用花括号 `{}`，键值对用冒号分隔 |
| 布尔值 | 使用 `true` 或 `false`（小写） |
| 数字 | 直接写数字，无需引号 |

**示例：**

features（数组）：

```json
["快速搭建", "多端适配", "权限管控"]
```

specifications（对象）：

```json
{"version": "2.0", "size": "15MB", "platform": "Web/Android/iOS"}
```

> **注意**：系统使用 `safeParse` 容错解析，JSON 解析失败时降级为空数组 `[]`，不会阻断保存，但内容会丢失。请保存前检查 JSON 语法。

### 7.11 常见问题与错误提示

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| JSON 字段保存失败 | JSON 语法错误（单引号、缺少逗号、括号不匹配等） | 检查 JSON 语法，使用双引号和正确的括号 |
| 发布后官网看不到内容 | status 未设置为 published | 在编辑页将状态改为「已发布」并保存 |
| 列表页或编辑页按钮不显示 | 当前用户角色权限不足 | 联系系统管理员分配相应 CT 的操作权限 |
| 上传封面图失败 | 文件过大或格式不支持 | 参考「媒体管理」章节的文件要求 |
| slug 重复导致保存失败 | 同一 CT 内 slug 必须唯一 | 修改 slug 或使用系统建议值 |

### 7.12 Admin UI 业务页面操作

除 7 个 CT 的标准 CRUD 外，官网中心还提供 6 个业务管理页面，位于 Admin UI 左侧菜单「网站管理」分区下。

#### 7.12.1 Dashboard 仪表盘

**功能**：站点核心运营数据总览。

| 模块 | 说明 |
|------|------|
| 文章总数卡片 | 已发布 article 数量 |
| 产品总数卡片 | 已发布 product 数量 |
| 线索总数卡片 | 留资 lead 数量（今日新增） |
| 搜索热度卡片 | 站内搜索次数（今日） |
| 线索 Tab | 留资列表，含姓名/公司/联系方式/来源/时间 |
| 搜索 Tab | 热门搜索词统计，含词/次数/占比 |

**操作**：登录后默认进入 Dashboard → 查看顶部 4 个统计卡片 → 点击「线索」Tab 查看留资列表 → 点击「搜索」Tab 查看热门搜索词 → 使用顶部时间筛选器切换「今日/7 天/30 天」。

#### 7.12.2 Studio Bridge 一键发布

**功能**：将 zhao-studio 的草稿一键发布为官网 article，建立双向关联。

**字段说明**：

| 字段名称 | 必填 | 类型 | 说明 |
|----------|------|------|------|
| draftId | * | string | Studio 草稿 ID |
| title | * | string | 发布后的文章标题 |
| category | * | relation | 关联 article-category |
| tags | | relation | 关联 tag（多选） |
| slug | * | uid | URL 标识 |

**操作**：左侧菜单点击「Studio Bridge」→ 填写 draftId/title/category/tags/slug → 点击「发布到官网」→ 等待 2-3 秒弹出「发布成功」→ 点击「查看文章」跳转 Article 详情页。

**深度演示参见** [tutorial.md 第 6 章](./tutorial.md)。

#### 7.12.3 Knowledge Graph 知识图谱

**功能**：管理站点实体与关系，输出 JSON-LD 结构化数据。

**实体字段**：

| 字段名称 | 必填 | 类型 | 说明 |
|----------|------|------|------|
| name | * | string | 实体名称 |
| entityType | * | enum | Organization/Person/Product/Event |
| description | | text | 实体描述 |
| url | | string | 实体链接 |
| properties | | json | 扩展属性 |

**操作**：左侧菜单点击「Knowledge Graph」→ 「+ 新建实体」填写字段保存 → 切换到「关系」Tab → 「+ 新建关系」选择源实体、关系类型、目标实体 → 点击右上角「导出 JSON-LD」复制 `@graph` 结构。

#### 7.12.4 First-Truth 真值管理

**功能**：管理站点关键事实的权威记录，用于冲突检测与 AI 摘要引用。

**真值字段**：

| 字段名称 | 必填 | 类型 | 说明 |
|----------|------|------|------|
| key | * | string | 真值键，如 foundingYear |
| value | * | string | 真值内容 |
| category | * | enum | business_license/company_basic/product_info |
| source | * | string | 数据来源 |
| confidence | * | float | 置信度 0-1 |
| verified | | boolean | 是否已验证 |

**操作**：左侧菜单点击「First-Truth」→ 「+ 新建真值」填写字段保存 → 切换到「冲突检测」Tab → 点击「扫描冲突」系统扫描所有内容与真值的字段，发现冲突时列表标红 → 对未验证真值点击「验证」按钮标记 verified=true。

#### 7.12.5 AI Summaries AI 摘要

**功能**：管理内容的 AI 自动摘要，支持编辑与重新生成。

**摘要字段**：

| 字段名称 | 必填 | 类型 | 说明 |
|----------|------|------|------|
| contentType | * | enum | article/product/case/faq/tutorial |
| contentTitle | * | string | 关联内容标题 |
| summary | * | text | 摘要正文 |
| updatedAt | | datetime | 最后更新时间 |

**操作**：左侧菜单点击「AI Summaries」→ 顶部筛选器选择 contentType → 查看摘要列表 → 点击「编辑」修改 summary 内容保存 → 如需重新生成点击「重新生成」按钮，等待 5-15 秒 AI 服务返回新摘要。

#### 7.12.6 SEO Output SEO 输出

**功能**：展示站点 3 类 SEO 输出文件，支持预览与下载。

**3 个 Tab**：

| Tab | 文件 | 说明 |
|-----|------|------|
| sitemap.xml | /sitemap.xml | 站点地图，含所有可收录 URL |
| robots.txt | /robots.txt | 爬虫协议，声明允许/禁止路径 |
| llms.txt | /llms.txt | LLM 友好摘要，供 AI 助手读取 |

**操作**：左侧菜单点击「SEO Output」→ 默认显示「sitemap.xml」Tab 查看预览 → 切换「robots.txt」确认 `Disallow: /api/` 与 Sitemap 声明 → 切换「llms.txt」查看站点摘要 → 点击右上角「下载」按钮可下载文件。

### 7.13 Studio Bridge 一键发布（深度流程）

Studio Bridge 是连接 zhao-studio（内容生产）与 zhao-website（内容发布）的桥梁，实现从草稿到上线的一键闭环。

**前置条件**：zhao-studio 已启动（`cd e:\code\studio && npm run dev`，访问 `http://localhost:3010`）。

**完整发布流程**：

1. 在 zhao-studio 撰写草稿并保存，记录草稿 ID（URL 中可见，如 `studio-draft-001`）
2. 切换到 Strapi Admin UI → 左侧菜单「Studio Bridge」
3. 填写发布参数：

| 字段 | 示例值 |
|------|--------|
| draftId | studio-draft-001 |
| title | 交互官网平台 2026 年度产品路线图 |
| category | 产品动态 |
| tags | 产品动态、内容管理 |
| slug | 2026-product-roadmap |

4. 点击「发布到官网」按钮
5. 等待 2-3 秒，弹出「发布成功」提示
6. 点击「查看文章」跳转 Article 详情页，确认以下字段：

| 字段 | 预期值 |
|------|--------|
| title | 交互官网平台 2026 年度产品路线图 |
| slug | 2026-product-roadmap |
| sourceType | studio |
| sourceId | studio-draft-001 |
| content | 来自 Studio 草稿的 HTML 转换结果 |

7. 切换回 zhao-studio 草稿列表，草稿右侧应显示「已发布到官网」标识与「查看官网文章」链接，形成双向关联

**技术原理**：
- **双向关联机制**：发布时分两步事务——第一步在 article 表创建记录写入 sourceType=studio、sourceId=draftId；第二步回写 zhao-studio 草稿的 websiteArticleId 字段。两步均成功才提交事务，否则回滚。
- **sourceType 溯源**：article 表的 sourceType 字段标识内容来源，取值 admin（Admin UI 直接创建）/studio（Studio Bridge 发布）/api（外部 API 创建）。Admin UI 可按来源筛选。
- **原子性回滚**：发布过程使用 Strapi 数据库事务包裹，任一步失败自动回滚。常见失败场景：草稿 ID 不存在、category 已删除、slug 重复。

### 7.14 CT 字段速查表

| CT | 必填字段 | 可选字段 | 关联 |
|----|----------|----------|------|
| product | name, tagline, slug, description, features | priceRange, specifications, scenarios, isFeatured | category, tags |
| case | title, clientName, clientIndustry, challenge, solution, results | testimonial, testimonialAuthor, testimonialTitle, isFeatured | tags |
| article | title, slug, excerpt, content, category, status | coverImage, tags, author, seoTitle, seoDescription, allowIndex, readingTime | category, tags |
| faq | question, answer, slug, status | order, isFeatured | category, tags |
| tutorial | title, slug, description, steps, estimatedTime, difficulty, status | coverImage, materials, result, isFeatured | category, tags |
| compliance | title, slug, category, content, status | effectiveDate, expiryDate, isPinned, seoTitle, seoDescription, allowIndex | 无 |
| download | name, description, fileType, requireLead, status | file, url, size, fileSize, downloadCount, isFeatured, order | category, tags |

**状态枚举速查**：

| 枚举类型 | 取值 | 说明 |
|----------|------|------|
| 内容状态（status） | draft / published / archived | 草稿 / 已发布 / 已下架 |
| 合规分类（category） | agreement / policy / certificate | 协议 / 政策 / 证书 |
| 教程难度（difficulty） | beginner / intermediate / advanced | 初级 / 中级 / 高级 |
| 渠道层级（channelTier） | root / official / partner / agent | 根 / 官方 / 合作伙伴 / 代理 |
| 渠道使用模式（channelUsage） | site_only_user / site_cross_user / channel_isolated | 仅站点 / 跨渠道（推荐）/ 渠道隔离 |

---

## 8. 题库管理

### 8.1 功能概述

题库管理用于管理系统中的题目，支持多种题型和难度级别。

### 8.2 创建题目

**操作步骤**：

1. 点击左侧菜单「题库管理」→「题目列表」
2. 点击「新建题目」按钮
3. 填写题目信息：

**题目信息字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 题目内容 | * | 题目的文本内容 |
| 题目类型 | * | single-choice（单选）/ multiple-choice（多选）/ true-false（判断）/ fill-blank（填空）/ essay（问答） |
| 难度等级 | | easy（简单）/ medium（中等）/ hard（困难） |
| 分数 | | 题目分值 |
| 正确答案 | * | 题目正确答案 |
| 选项 | | 选择题需要填写选项 |
| 解释说明 | | 题目解析（可选） |

4. 点击「保存」按钮

### 8.3 批量导入题目

**操作步骤**：

1. 点击左侧菜单「题库管理」→「批量导入」
2. 下载导入模板
3. 按照模板格式填写题目数据
4. 上传填写好的文件
5. 点击「导入」按钮

### 8.4 考试管理

**操作步骤**：

1. 点击左侧菜单「题库管理」→「考试管理」
2. 点击「新建考试」按钮
3. 填写考试信息：

**考试信息字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 考试名称 | * | 考试的显示名称 |
| 描述 | | 考试说明 |
| 题目来源 | | 从题库中选择题目 |
| 时间限制 | | 考试时长（分钟） |
| 及格分数 | | 及格线分数 |

4. 点击「保存」按钮

### 8.5 查看答题记录

**操作步骤**：

1. 点击左侧菜单「题库管理」→「答题记录」
2. 选择要查看的记录
3. 查看答题详情和得分

---

## 9. 积分管理

### 9.1 功能概述

积分管理用于管理系统的积分体系，包括积分类型、规则、记录和兑换。

### 9.2 积分类型管理

**操作步骤**：

1. 点击左侧菜单「积分管理」→「积分类型」
2. 点击「新建积分类型」按钮
3. 填写积分类型信息：

**积分类型字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 类型代码 | * | 唯一标识，如 "study_points" |
| 类型名称 | * | 显示名称，如 "学习积分" |
| 描述 | | 类型说明 |
| 是否启用 | | 默认启用 |
| 是否可过期 | | 默认启用 |
| 过期天数 | | 默认 365 天 |

4. 点击「保存」按钮

### 9.3 积分规则管理

**操作步骤**：

1. 点击左侧菜单「积分管理」→「积分规则」
2. 点击「新建规则」按钮
3. 填写规则信息：

**规则信息字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 积分类型 | | 选择积分类型 |
| 积分数 | | 奖励积分数量 |
| 条件 | | 触发条件（可选） |
| 限制 | | 使用限制（可选） |
| 有效期 | | 规则生效时间（可选） |

4. 点击「保存」按钮

### 9.4 积分记录查看

**操作步骤**：

1. 点击左侧菜单「积分管理」→「积分记录」
2. 使用筛选条件查找记录：
   - 用户ID
   - 积分类型
   - 时间范围
3. 查看积分变更记录

### 9.5 积分兑换管理

**操作步骤**：

1. 点击左侧菜单「积分管理」→「兑换管理」
2. 点击「新建兑换商品」按钮
3. 填写商品信息：

**商品信息字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 商品名称 | * | 兑换商品名称 |
| 描述 | | 商品说明 |
| 所需积分 | * | 兑换所需积分 |
| 库存 | | 商品数量 |

4. 点击「保存」按钮

### 9.6 积分统计

**操作步骤**：

1. 点击左侧菜单「积分管理」→「统计报表」
2. 查看积分发放、消费和余额统计
3. 使用时间筛选器查看不同时间段的数据

---

## 10. 系统管理

### 10.1 功能概述

系统管理用于管理系统的用户、角色和权限配置。

### 10.2 用户管理

**操作步骤**：

1. 点击左侧菜单「系统管理」→「用户管理」
2. 查看用户列表
3. 点击「编辑」修改用户信息
4. 点击「删除」删除用户
5. 点击「重置密码」重置用户密码

### 10.3 角色管理

**操作步骤**：

1. 点击左侧菜单「系统管理」→「角色管理」
2. 查看角色列表
3. 点击「编辑」修改角色权限
4. 点击「新建角色」创建新角色
5. 设置角色权限：
   - 选择要授予的权限
   - 保存角色配置

### 10.4 权限管理

**操作步骤**：

1. 点击左侧菜单「系统管理」→「权限管理」
2. 查看所有权限列表
3. 搜索权限
4. 查看权限详情

### 10.5 用户角色分配

**操作步骤**：

1. 点击左侧菜单「系统管理」→「用户角色」
2. 选择用户
3. 为用户分配角色
4. 保存配置

---

## 11. 媒体管理

### 11.1 功能概述

媒体管理用于管理系统中的图片和文件资源。

### 11.2 上传文件

**操作步骤**：

1. 点击左侧菜单「媒体管理」→「媒体库」
2. 点击「上传文件」按钮
3. 选择要上传的文件
4. 等待上传完成
5. 文件会自动保存到媒体库中

### 11.3 管理文件

**操作步骤**：

1. 在媒体库中找到要管理的文件
2. 点击「预览」查看文件
3. 点击「编辑」修改文件信息
4. 点击「删除」删除文件

### 11.4 使用文件

在编辑课程、租户等内容时，可以从媒体库中选择已上传的文件作为封面图片或其他用途。

---

## 12. 第三方配置

### 12.1 功能概述

第三方配置用于配置系统与外部服务的集成，如微信登录、微信分享等。

### 12.2 微信配置

**操作步骤**：

1. 点击左侧菜单「第三方配置」→「微信配置」
2. 填写微信公众号信息：

**微信公众号字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| AppID | * | 微信公众号 AppID |
| AppSecret | * | 微信公众号 AppSecret |
| Token | | 服务器配置 Token |
| EncodingAESKey | | 消息加密密钥（可选） |

3. 配置授权域名：
   - 网页授权域名
   - JS-SDK 安全域名
4. 点击「保存」按钮

### 12.3 微信分享配置

**操作步骤**：

1. 在微信配置页面中找到「分享配置」区域
2. 填写分享信息：

**分享配置字段说明：**

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 分享标题 | | 默认分享标题 |
| 分享描述 | | 默认分享描述 |
| 分享路径 | | 默认分享页面路径 |

3. 点击「保存」按钮

### 12.4 SSO 统一登录管理（zhao-sso 插件）

zhao-sso 插件提供独立于多租户三方配置的统一登录能力，所有接入方共享同一套微信公众号配置，应用之间通过 app_code 隔离。入口：`http://h.joho.cn/admin/plugins/zhao-sso`。

#### 12.4.1 与多租户三方配置的区别

| 维度 | 多租户三方配置（12.2/12.3） | SSO 统一登录（zhao-sso） |
|------|------------------------------|---------------------------|
| 配置作用域 | 每个租户独立配置 | 全局共享，所有接入方复用 |
| 用户体系 | users-permissions.user | sso-user |
| 应用隔离 | siteId | app_code |
| 公众号 appId | 各租户可不同 | 全局唯一 |
| 适用场景 | 单租户独立登录 | 多应用统一登录、跨应用 SSO |

#### 12.4.2 OAuth 配置管理

OAuth 配置是 SSO 的核心，定义微信公众号/开放平台凭证。

**操作步骤**：

1. 进入「SSO 统一登录 → OAuth配置」Tab
2. 点击「新建」填写配置

**字段说明**：

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| 配置名称 | * | 配置的显示名称（如 "h.joho.cn 微信公众号"） |
| 平台 | * | wechat / alipay / douyin / google / github |
| 应用类型 | * | official_account / open_platform / mini_program / app / default |
| AppID | * | 对应平台的应用 ID |
| AppSecret | * | 对应平台的应用密钥 |
| OAuth Scope | * | 多选：snsapi_userinfo / snsapi_base（微信公众号必选） |
| 是否启用 | | 启用后接入方可使用 |
| redirect_uris | | 回调白名单（逗号分隔），如 `http://h.joho.cn/api/zhao-sso/v1/auth/wechat/callback` |
| extra_config | | JSON 扩展配置（如 Token、EncodingAESKey） |

**关键约束**：

- 微信公众号必须是认证服务号（订阅号无网页授权权限）
- AppType 决定授权形态：official_account=网页授权、open_platform=扫码登录、mini_program=jscode2session、app=APP SDK
- OAuth Scope 多选后，前端按场景选用（snsapi_base 静默登录、snsapi_userinfo 完善资料）

#### 12.4.3 SSO 应用管理（sso-app）

每个接入 SSO 的业务方都需要注册为应用，获取独立的 app_code/app_secret。

**操作步骤**：

1. 进入「SSO 统一登录 → 应用管理」Tab
2. 点击「新建」填写应用信息

**字段说明**：

| 字段名称 | 必填 | 说明 |
|----------|------|------|
| app_code | * | 应用唯一标识（如 admin、course、mystore） |
| app_name | * | 应用显示名称 |
| app_secret | * | 应用密钥，前端换 token 时需传入，妥善保存 |
| redirect_uris | * | OAuth 回调地址白名单（多个用逗号分隔），必须与前端实际 URL 完全一致 |
| allowed_grant_types | | authorization_code, refresh_token（默认） |
| is_active | | 启用/禁用应用 |

**预置应用**：

| app_code | 用途 | redirect_uri 示例 |
|----------|------|-------------------|
| admin | 后台管理（h.joho.cn） | `http://h.joho.cn/#/pages/login/callback` |
| course | C 端课程（v.joho.cn） | `http://v.joho.cn/#/pages/login/callback` |

> **重要**：redirect_uri 必须与前端实际请求的 URL 完全一致（包括协议、端口、路径、hash）。`http://` 与 `https://`、`localhost` 与 `127.0.0.1`、带尾斜杠与不带尾斜杠均视为不同。

#### 12.4.4 三方绑定管理

三方绑定记录 sso-user 与第三方平台（微信/支付宝等）的 openid 对应关系，一个 sso-user 可绑定多个第三方。

**操作步骤**：

1. 进入「SSO 统一登录 → 三方绑定」Tab
2. 查看已绑定记录，支持按用户/平台筛选
3. 点击「删除」可解绑某个第三方账号

#### 12.4.5 渠道与分销管理

SSO 内置 channel-sync 服务，处理邀请码/渠道码的分销关系建立，独立于 zhao-auth。

**参数来源**：

| 参数 | 来源域名 | 用途 |
|------|----------|------|
| invite_code | v.joho.cn | 建立 sso-user 分销关系 |
| channel_code | h.joho.cn | 建立 sso-user 分销关系 |

**流程**：

1. 用户访问 `https://mystore.example.com/?invite_code=ABC123&channel_code=XYZ789#/pages/login/index`
2. 前端解析 URL 参数，透传给 wx-sso-login 组件
3. 组件调用 `/api/zhao-sso/v1/auth/password-authorize` 或 `/api/zhao-sso/v1/auth/wechat` 时附带 invite_code/channel_code
4. SSO channel-sync 服务建立 sso-user 分销关系（sso_referral_relations，level 无上限）
5. 邀请码校验失败不阻塞登录，仅记录日志

#### 12.4.6 Nginx 配置要点

SSO 正常工作依赖 Nginx 正确透传代理头，详见 `d:\zhao\strapi\docs\deployment\nginx-h-joho-cn.conf`。

**关键配置**：

```nginx
# 全局代理头（X-Forwarded-Host 必须用 $host 而非 $server_name）
proxy_set_header Host $host;
proxy_set_header X-Forwarded-Host $host;
proxy_set_header X-Forwarded-Proto $scheme;

# /api/ 反代已覆盖所有 SSO 接口
location /api/ {
    proxy_pass http://127.0.0.1:1337/api/;
}
```

**常见问题**：

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| SSO 回调 302 重定向到 localhost | Nginx 未透传 X-Forwarded-Host | 改用 `$host` 而非 `$server_name` |
| HTTPS 环境回调 URL 协议错误 | 未设置 X-Forwarded-Proto | 添加 `proxy_set_header X-Forwarded-Proto $scheme;` |
| JSSDK 签名失败 | JS接口安全域名未配置 | 公众号后台「功能设置 → JS接口安全域名」填 `h.joho.cn` |
| 网页授权报 redirect_uri 参数错误 | 网页授权域名未配置 | 公众号后台「功能设置 → 网页授权域名」填 `h.joho.cn` |

#### 12.4.7 接口速查

**公开接口（无需认证）**：

| 接口 | 方法 | 路径 |
|------|------|------|
| 微信授权跳转 | GET | `/api/zhao-sso/v1/auth/wechat` |
| 微信中转回调 | GET | `/api/zhao-sso/v1/auth/wechat/callback` |
| 小程序登录 | POST | `/api/zhao-sso/v1/auth/wechat/miniprogram` |
| APP 登录 | POST | `/api/zhao-sso/v1/auth/wechat/app` |
| JSSDK 签名 | POST | `/api/zhao-sso/v1/auth/jssdk-signature` |
| 微信登录配置 | GET | `/api/zhao-sso/v1/auth/wechat/config` |
| 降级密码登录（code 模式） | POST | `/api/zhao-sso/v1/auth/password-authorize` |
| 降级密码登录（token 模式） | POST | `/api/zhao-sso/v1/auth/login` |
| 换取 token | POST | `/api/zhao-sso/v1/auth/token` |

**管理接口（需认证）**：

| 接口 | 方法 | 路径 |
|------|------|------|
| OAuth 配置列表 | GET | `/api/zhao-sso/v1/admin/oauth-configs` |
| 新建 OAuth 配置 | POST | `/api/zhao-sso/v1/admin/oauth-configs` |
| 三方绑定列表 | GET | `/api/zhao-sso/v1/admin/bindings` |
| 删除三方绑定 | DELETE | `/api/zhao-sso/v1/admin/bindings/:id` |

> 完整配置流程、前端接入代码、故障排查详见 `d:\zhao\strapi\docs\deployment\sso-wechat-config-guide.md`。

---

## 13. 常见问题

### 13.1 登录失败

**问题**：输入用户名和密码后无法登录

**排查步骤**：

1. 检查用户名和密码是否正确
2. 检查网络连接是否正常
3. 检查服务器是否正常运行
4. 联系系统管理员重置密码

### 13.2 页面空白

**问题**：登录后页面显示空白

**排查步骤**：

1. 刷新页面
2. 清除浏览器缓存
3. 检查浏览器版本是否符合要求
4. 检查控制台是否有错误信息

### 13.3 权限不足

**问题**：无法访问某些菜单或执行某些操作

**排查步骤**：

1. 检查当前用户角色
2. 联系管理员获取更高权限
3. 确认是否需要先完成其他操作

### 13.4 文件上传失败

**问题**：上传图片或文件失败

**排查步骤**：

1. 检查文件大小是否超过限制
2. 检查文件格式是否支持
3. 检查网络连接是否正常
4. 联系管理员检查服务器存储配置

### 13.5 数据不显示

**问题**：页面不显示数据

**排查步骤**：

1. 检查筛选条件是否正确
2. 检查是否有权限查看该数据
3. 检查网络连接是否正常
4. 检查服务器是否正常运行

### 13.6 租户保存失败

**问题**：创建或编辑租户时保存失败

**排查步骤**：

1. 检查租户名称和域名是否填写
2. 检查是否选择了至少一个渠道
3. 检查表单中的必填项是否都已填写
4. 检查网络连接是否正常

### 13.7 课程发布后不可见

**问题**：课程发布后在前端不可见

**排查步骤**：

1. 检查课程状态是否为「已发布」
2. 检查课程的渠道范围设置是否正确
3. 检查是否设置了正确的积分归属渠道
4. 检查课程的时间设置是否在有效期内

### 13.8 渠道无法删除

**问题**：尝试删除渠道时失败

**排查步骤**：

1. 检查渠道下是否有子渠道，先删除子渠道
2. 检查渠道下是否有成员，先移除成员
3. 检查渠道是否被课程或其他资源引用

---

## 14. 官网中心学习路径

本章按「初级 → 中级 → 高级」三阶段组织官网中心的学习路径，每阶段明确前端与后台任务，帮助使用者快速上手并深度使用产品。详细操作示例与完整 JSON 数据参见 [tutorial.md](./tutorial.md)。

### 14.1 初级路径（快速上手，30 分钟）

**目标**：完成首次登录、创建首篇内容、前端访问验证，建立产品整体认知。

**后台任务**：

| 步骤 | 操作 | 参考章节 |
|------|------|----------|
| 1 | 启动 Strapi（`cd e:\code\basic && npm run develop`） | tutorial.md 1.2 |
| 2 | 访问 `http://localhost:1337/admin` 注册管理员账号 | tutorial.md 1.4 |
| 3 | 进入「网站管理 → 资讯文章」→「+ 新增文章」 | 本手册 7.3 |
| 4 | 填写标题、正文、分类，点击「发布」 | 本手册 7.2 |
| 5 | 在列表页确认文章状态为「已发布」 | 本手册 7.2 |

**前端任务**：

| 步骤 | 操作 | 参考章节 |
|------|------|----------|
| 1 | 启动 dsite（`cd e:\code\dsite && npm run dev`） | tutorial.md 7.1 |
| 2 | 浏览器访问 `http://localhost:3000` 验证首页 | tutorial.md 7.3 |
| 3 | 访问 `/articles/{slug}` 查看刚发布的文章 | tutorial.md 7.3 |
| 4 | 右键「查看页面源代码」确认 SSR 渲染（含完整正文 HTML） | tutorial.md 7.4 |

**验收标准**：
- 后台文章列表存在 1 条已发布文章
- 前端首页正常渲染
- 文章详情页源代码含完整正文（非空 div）

### 14.2 中级路径（完整配置，2 小时）

**目标**：完成多租户配置、7 个 CT 内容创建、Admin UI 6 个业务页面操作，覆盖官网中心全部基础能力。

**后台任务**：

| 步骤 | 操作 | 参考章节 |
|------|------|----------|
| 1 | 编辑 site-config（siteName/domain/icpNumber/seoKeywords） | tutorial.md 2.1 |
| 2 | 关联模板（default/minimal/business/tech/education） | tutorial.md 2.2 |
| 3 | 配置 featureFlags（按需开启 channel/oss 等） | tutorial.md 2.3 |
| 4 | 创建 channel 并关联到 site-config | tutorial.md 2.4-2.5 |
| 5 | 创建 5 个 article-category 与 15 个 tag | tutorial.md 3.1-3.2 |
| 6 | 创建 7 个 CT 内容（product×1/case×3/article×5/faq×8/tutorial×6/compliance×8/download×3） | tutorial.md 4.1-4.7 |
| 7 | 操作 Dashboard 查看统计卡片与线索/搜索 Tab | 本手册 7.12.1 |
| 8 | 操作 Knowledge Graph 创建实体与关系，导出 JSON-LD | 本手册 7.12.3 |
| 9 | 操作 First-Truth 创建真值并执行冲突检测 | 本手册 7.12.4 |
| 10 | 操作 AI Summaries 查看与重新生成摘要 | 本手册 7.12.5 |
| 11 | 操作 SEO Output 预览 sitemap/robots/llms | 本手册 7.12.6 |

**前端任务**：

| 步骤 | 操作 | 参考章节 |
|------|------|----------|
| 1 | 验证 16 个路由全部可访问 | tutorial.md 7.3 |
| 2 | 访问 `/sitemap.xml` 确认含所有可收录 URL | tutorial.md 7.4 |
| 3 | 访问 `/robots.txt` 确认 `Disallow: /api/` | tutorial.md 7.4 |
| 4 | 访问 `/llms.txt` 确认含站点摘要 | tutorial.md 7.4 |
| 5 | 在文章页源代码确认 meta 标签（title/description/og/json-ld） | tutorial.md 7.4 |
| 6 | 在 `/contact` 提交留资表单，Dashboard 线索 Tab 验证 | tutorial.md 7.5 |
| 7 | 下载 requireLead=true 资源验证留资流程 | tutorial.md 7.6.1 |
| 8 | 下载 requireLead=false 资源验证直链下载 | tutorial.md 7.6.2 |

**验收标准**：
- 后台 7 个 CT 共 34 条已发布内容
- Admin UI 6 个业务页面均可正常操作
- 前端 16 个路由全部 200 状态
- SEO 输出 3 个文件均可访问
- 留资表单提交后 Dashboard 有记录

### 14.3 高级路径（深度定制，1 天）

**目标**：掌握 Studio Bridge 双向关联、自定义模板开发、API 集成、权限配置、性能优化等深度能力。

**后台任务**：

| 步骤 | 操作 | 参考章节 |
|------|------|----------|
| 1 | 启动 zhao-studio 撰写草稿 | tutorial.md 6.1-6.2 |
| 2 | 通过 Studio Bridge 一键发布，验证双向关联 | 本手册 7.13 |
| 3 | 配置 Strapi RBAC 角色（super-admin/admin/editor/viewer） | tutorial.md 4.5.6 |
| 4 | 创建 API Token，测试 REST API 认证与 CRUD | tutorial.md 4.5.4 |
| 5 | 配置 Webhook 推送留资线索到外部 CRM | tutorial.md 4.4.9 |
| 6 | 深度使用 Knowledge Graph 构建完整实体关系网 | 本手册 7.12.3 |
| 7 | 深度使用 First-Truth 建立真值库并持续冲突检测 | 本手册 7.12.4 |
| 8 | 深度使用 AI Summaries 批量生成与编辑摘要 | 本手册 7.12.5 |

**前端任务**：

| 步骤 | 操作 | 参考章节 |
|------|------|----------|
| 1 | 开发自定义模板（config.json/theme.json/index.vue） | tutorial.md 4.5.3 |
| 2 | 注册模板并关联到 site-config 验证渲染 | tutorial.md 4.5.3 |
| 3 | 启用 Redis 缓存热点数据（site-info/模板配置） | tutorial.md 4.5.6 |
| 4 | 配置 Nuxt routeRules ISR 增量再生 | tutorial.md 4.5.6 |
| 5 | 开启 OSS 图片加速与 CDN 回源 | tutorial.md 4.5.6 |
| 6 | 启用 nitro compressPublicAssets 资源压缩 | tutorial.md 4.5.6 |
| 7 | 接入 Prometheus + Grafana 监控告警 | tutorial.md 4.5.6 |

**验收标准**：
- Studio Bridge 发布的 article 含 sourceType=studio、sourceId 字段
- zhao-studio 草稿显示「已发布到官网」标识
- 自定义模板可被 site-config 关联并正常渲染
- API Token 可通过 curl 完成内容 CRUD
- P95 延迟 < 200ms，QPS 1000+，首屏加载 < 1s

### 14.4 学习路径总结

| 阶段 | 耗时 | 后台核心 | 前端核心 | 产物 |
|------|------|----------|----------|------|
| 初级 | 30 分钟 | 登录 + 创建首篇文章 | 访问首页与文章详情 | 1 篇已发布文章 + 可访问前端 |
| 中级 | 2 小时 | 多租户配置 + 7 个 CT + 6 个业务页面 | 16 路由验收 + SEO + 留资 | 完整官网中心 + 34 条内容 |
| 高级 | 1 天 | Studio Bridge + 权限 + API + 深度业务页面 | 自定义模板 + 性能优化 + 监控 | 可生产的深度定制官网 |

**推荐学习顺序**：严格按初级 → 中级 → 高级递进，每阶段验收通过后再进入下一阶段。跳级可能导致配置缺失或理解断层。

**深度教程**：本手册第 7 章扩展节与第 14 章为速查与路径引导，完整字段表、JSON 数据示例、截图位标注、技术原理详解参见 [tutorial.md](./tutorial.md)（2630 行完整教程）。

---

## 附录：快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl + S | 保存当前表单 |
| Ctrl + F | 搜索 |
| Esc | 关闭弹窗 |

## 附录：字段单位说明

| 字段类型 | 单位 | 说明 |
|----------|------|------|
| 价格 | 分 | 如 100 分 = 1 元 |
| 时长 | 分钟 | 课程或课时的总时长 |
| 积分 | 个 | 积分数量 |
| 分数 | 分 | 考试或题目的分值 |

## 附录：状态说明

### 课程状态

| 状态代码 | 显示名称 | 说明 |
|----------|----------|------|
| draft | 草稿 | 课程正在编辑中，不可见 |
| pending | 待审核 | 课程提交审核，等待审核通过 |
| published | 已发布 | 课程已发布，对用户可见 |
| archived | 已归档 | 课程已归档，不再展示 |

### 审核状态

| 状态代码 | 显示名称 | 说明 |
|----------|----------|------|
| pending | 待审核 | 等待审核 |
| approved | 已通过 | 审核通过 |
| rejected | 已拒绝 | 审核拒绝 |